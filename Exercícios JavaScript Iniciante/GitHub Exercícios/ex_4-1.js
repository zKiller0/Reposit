// Escreva um programa que calcula e mostre a tabuada (até 10x) de um número qualquer, digitado pelo usuário.

let = parseInt ( window.prompt ( 'Digite o valor da tabuada: ' ) );

for ( count = 1 ; count <= 10 ; count++ ) {

    document.write ( valor + ' X ' + count + ' = ' + ( count * valor ) + '<br>' ); 

}

// Não exibe na tela o resultado