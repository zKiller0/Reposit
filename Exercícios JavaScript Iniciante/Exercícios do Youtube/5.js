// Entrada
let nome = prompt ( 'Digite seu nome: ' );

// Impressão
alert ( `Seu nome e ${nome}` ); 

// Entrada
let numero = parseInt ( prompt ( 'Digite um numero: ' ) );

// Calculo
let soma = numero + 2;

// Impressão
alert ( `O numero digitado foi ${numero} e a soma +2 e ${soma}` );

// Transforamar a entrada do prompt em número
// Numero Inteiro = parseInt 
// Numero real = parseFloat
