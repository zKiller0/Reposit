// Entendendo o Contador

// Contador
let Contador = 0;

// Repetição Contada
while ( Contador < 10 ) { // De 0 até 9
    alert ( `Contador` );

    Contador = Contador + 1;
    // De 1 até 10

    /* Colocando o alert antes da operação,
     mostra o valor de 0 até 9,
     e depois da operação o valor de 1 até 10. */
}
