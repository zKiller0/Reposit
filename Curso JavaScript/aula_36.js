// Combinando e Cortando Arrays

const primeiro = [ 1 , 2 , 3 ];
const segundo = [ 4 , 5 , 6 ];

// Combinar 
const combinado = primeiro.concat ( segundo );
console.log ( combinado );

// Dividir 
const  cortado = combinado.slice (  );
console.log ( cortado );

//

const consoantes = [ 'b' , 'c' , 'd' , 'f' ];
const vogal = [ 'a' , 'e' , 'i' , 'o' , 'u' ];

const combinado1 = consoantes.concat ( vogal )

console.log ( combinado1 )