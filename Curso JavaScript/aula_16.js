// Laço / Loop

// For

for ( let i = 0; i < 100 ; i ++ ) {
    if ( i % 2  !== 0 ) {  
        // Modules
        console.log (i);
    }
}
// Imprimindo so os valores ímpares
/*for ( let i = 0; i < 100 ; i ++ ) {  // -- = do maior para o menor
                                          ++ = do menor para o maior
    if ( i % 2  !== 0 ) {  
        // Modules           
        console.log (i);
    }
}
*/