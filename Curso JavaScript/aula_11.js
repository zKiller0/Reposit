// Operador Ternário

// Tem um cliente, 100 pontos = premium, se não, commu

let pontos = 100;
let tipo = pontos > 100 ? 'Premium' : 'Comum';
//         expressão caso seja: verdadeiro | falso
console.log ( tipo );