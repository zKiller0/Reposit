// While

for ( let i = 5; i >=1; i -- ) {
    if ( i % 2  !== 0 ) {  
        // Modules
        console.log (i);
    }
}

let i = 5;

while ( i >= 1 )/* Condição ( ) */ {
    if ( i % 2 !== 0 ) {
        console.log (i);
    }
    i--;
}
